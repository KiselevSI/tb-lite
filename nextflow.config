manifest {
    name            = 'tb-lite'
    author          = 'Kiselev S.I.'
    description     = 'Mycobacterium tuberculosis WGS analysis pipeline'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=23.04.0'
    version         = '2.0.0'
    defaultBranch   = 'main'
}

params {
    input               = null
    samples             = null
    sra_ids             = null
    outdir              = "./results"
    reference           = "${baseDir}/assets/h37rv.fa"
    mode                = 'copy'

    // Reporting
    skip_reports        = false
    skip_multiqc        = false
    skip_final_reports  = false
    skip_snp_matrix     = false
    skip_qc             = false

    // Optional Kraken2/Bracken
    skip_kraken         = false
    kraken2_db          = null

    // Assets
    rd_db               = "${baseDir}/assets/rd/RD.bed"
    is6110              = "${baseDir}/assets/ismap/is6110.fasta"
    ref_gbk             = "${baseDir}/assets/ismap/h37rv.gbk"
    chr_name            = "${baseDir}/assets/chr_name/chr.txt"
    levels              = "${baseDir}/assets/tbmix/levels.tsv"
    feature_table       = "${baseDir}/assets/vcf2table/feature_table.tsv"
    multiqc             = null
    multiqc_config      = "${baseDir}/assets/multiqc/multiqc_config.yaml"
    gff                 = null
    snpeff_db           = "Mycobacterium_tuberculosis_h37rv"
    snpeff_data_dir     = "${baseDir}/snpEff_latest_core/snpEff/data"
    gbk                 = "${baseDir}/assets/h37rv.gb"
    h37rv_feature_table = "${baseDir}/assets/SNPEFF_ANNOTATION/h37rv_feature_table.txt"

    // Filters
    min_align_pct       = 90
    min_mean_cov        = 10
    min_median          = 30

    sifDir              = (System.getenv('NXF_SINGULARITY_CACHEDIR') ?: "${baseDir}/containers")
    help                = false
}

includeConfig 'conf/base.config'
includeConfig 'conf/modules.config'

executor {
    name = 'local'
    cpus = 12
}

process {
    withLabel: solo_cpu {
        maxForks = 12
    }
    withLabel: multi_cpu {
        maxForks = 6
        cpus     = 4
    }
}

profiles {
    docker {
        docker.enabled      = true
        singularity.enabled = false
        conda.enabled       = false
    }

    singularity {
        singularity.enabled    = true
        singularity.autoMounts = true
        docker.enabled         = false
        conda.enabled          = false
    }

    conda {
        conda.enabled       = true
        docker.enabled      = false
        singularity.enabled = false
    }
}

docker {
    enabled    = true
    runOptions = '-u $(id -u):$(id -g)'
}

singularity {
    enabled    = false
    autoMounts = true
}
